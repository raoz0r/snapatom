<Window x:Class="Text_Grab.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SnapAtom Settings" Height="700" Width="580"
        WindowStartupLocation="CenterScreen"
        Background="#18181B" Foreground="#F4F4F5"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" BorderThickness="1" BorderBrush="#27272A"
        Icon="pack://siteoforigin:,,,/app_icon.png">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="Foreground" Value="#F4F4F5"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}" 
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" Value="#52525B"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter Property="BorderBrush" Value="#A855F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="Foreground" Value="#F4F4F5"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                           BorderBrush="{TemplateBinding BorderBrush}" 
                                           Background="{TemplateBinding Background}" 
                                           Foreground="{TemplateBinding Foreground}" 
                                           Grid.Column="2" 
                                           Focusable="false"
                                           IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                           ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                CornerRadius="6" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="24" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" 
                                                      Grid.Column="1" 
                                                      Fill="#A1A1AA" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="#3F3F46" />
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#52525B" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="#18181B" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" 
                                               IsHitTestVisible="False" 
                                               Content="{TemplateBinding SelectionBoxItem}"
                                               ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                               ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                               Margin="8,3,28,3" 
                                               VerticalAlignment="Center" 
                                               HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" 
                                       SnapsToDevicePixels="True" 
                                       MinWidth="{TemplateBinding ActualWidth}" 
                                       MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" 
                                             Background="#18181B" 
                                             BorderBrush="#3F3F46" 
                                             BorderThickness="1" 
                                             CornerRadius="6" 
                                             Margin="0,2,0,0"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="#F4F4F5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                CornerRadius="4" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="#A855F7"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="#7E22CE"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D4D4D8"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Premium TabControl styling -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A1A1AA"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="contentPresenter" ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Foreground" Value="#F4F4F5"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Foreground" Value="#A855F7"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#A855F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Scrollbar Styling -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                                    <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Focusable="false" Background="Transparent" BorderThickness="0" Height="0" Width="0"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" Focusable="false" Background="Transparent" BorderThickness="0" Height="0" Width="0"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Background="#3F3F46">
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="PART_Track" Property="Thumb.Background" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                                    <Track x:Name="PART_Track" IsDirectionReversed="false" IsEnabled="{TemplateBinding IsEnabled}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.LineLeftCommand}" Focusable="false" Background="Transparent" BorderThickness="0" Height="0" Width="0"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.LineRightCommand}" Focusable="false" Background="Transparent" BorderThickness="0" Height="0" Width="0"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Background="#3F3F46">
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="PART_Track" Property="Thumb.Background" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Title Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Content Tabs -->
            <RowDefinition Height="*"/>
            <!-- Footer Buttons -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E1E24" Padding="20,20,20,15" MouseDown="Header_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://siteoforigin:,,,/app_icon.png" Width="36" Height="36" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="SnapAtom Settings" FontSize="20" FontWeight="Bold" Foreground="#FAF5FF"/>
                        <TextBlock Text="Configure providers, paths, shortcuts, and AI features" FontSize="12" Foreground="#A1A1AA" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="1" Content="✕" Width="30" Height="30" Background="Transparent" Foreground="#A1A1AA" BorderThickness="0" FontSize="16" Click="CancelButton_Click" Cursor="Hand" VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter Property="Foreground" Value="#EF4444"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>        <!-- Main Form with Tabs -->
        <TabControl Grid.Row="1" Margin="15,5,8,5">
            <!-- Tab 1: General -->
            <TabItem Header="General">
                <StackPanel Margin="0,15,4,0">
                    <!-- Section: Output Location -->
                    <TextBlock Text="Output Location" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Clippings Save Path" FontSize="12" Foreground="#D4D4D8" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="SavePathTextBox" Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="Browse..." Width="80" Style="{StaticResource SecondaryButton}" Click="BrowseButton_Click"/>
                    </Grid>

                    <!-- Section: Custom Metadata -->
                    <Separator Background="#27272A" Margin="0,10,0,15"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Custom Metadata (Frontmatter)" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC"/>
                            <TextBlock Text="Additional key-value pairs added to note frontmatter." FontSize="11" Foreground="#A1A1AA" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="+ Add Key" Style="{StaticResource SecondaryButton}" Click="AddMetadataButton_Click" VerticalAlignment="Center"/>
                    </Grid>

                    <ScrollViewer Height="180" VerticalScrollBarVisibility="Auto" BorderBrush="#27272A" BorderThickness="1" Background="#0F0F11" Margin="0,0,0,10">
                        <ItemsControl x:Name="MetadataItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0" Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" ToolTip="Metadata Key (e.g. author)"/>
                                        <TextBox Grid.Column="1" Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" ToolTip="Metadata Value (e.g. Obsidian)"/>
                                        <Button Grid.Column="2" Width="32" Height="32" Style="{StaticResource SecondaryButton}" Click="DeleteMetadataButton_Click" Tag="{Binding}" ToolTip="Delete metadata item">
                                            <Viewbox Width="14" Height="14">
                                                <Path Data="M 10,11 V 17 M 14,11 V 17 M 19,6 V 20 A 2,2 0 0 1 17,22 H 7 A 2,2 0 0 1 5,20 V 6 M 3,6 H 21 M 8,6 V 4 A 2,2 0 0 1 10,2 H 14 A 2,2 0 0 1 16,4 V 6"
                                                      Stroke="#EF4444" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"/>
                                            </Viewbox>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </TabItem>

            <!-- Tab 2: Keyboard Shortcuts -->
            <TabItem Header="Shortcuts">
                <StackPanel Margin="0,15,4,0">
                    <TextBlock Text="Global Keyboard Shortcuts" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC" Margin="0,0,0,10"/>
                    
                    <!-- Start Grab Hotkey -->
                    <Border BorderBrush="#27272A" BorderThickness="1" CornerRadius="8" Padding="12" Background="#1E1E24" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Capture Region Overlay" FontWeight="SemiBold" FontSize="12" Foreground="#E4E4E7" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <CheckBox x:Name="GrabWinCb" Content="Win"/>
                                    <CheckBox x:Name="GrabCtrlCb" Content="Ctrl"/>
                                    <CheckBox x:Name="GrabShiftCb" Content="Shift"/>
                                    <CheckBox x:Name="GrabAltCb" Content="Alt"/>
                                </StackPanel>
                                <ComboBox Grid.Column="1" x:Name="GrabKeyCombo" Width="80"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Copy Selected Hotkey -->
                    <Border BorderBrush="#27272A" BorderThickness="1" CornerRadius="8" Padding="12" Background="#1E1E24" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Copy Selected Text" FontWeight="SemiBold" FontSize="12" Foreground="#E4E4E7" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <CheckBox x:Name="CopyWinCb" Content="Win"/>
                                    <CheckBox x:Name="CopyCtrlCb" Content="Ctrl"/>
                                    <CheckBox x:Name="CopyShiftCb" Content="Shift"/>
                                    <CheckBox x:Name="CopyAltCb" Content="Alt"/>
                                </StackPanel>
                                <ComboBox Grid.Column="1" x:Name="CopyKeyCombo" Width="80"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Process Batch Hotkey -->
                    <Border BorderBrush="#27272A" BorderThickness="1" CornerRadius="8" Padding="12" Background="#1E1E24" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Process Batch / Run Pipeline" FontWeight="SemiBold" FontSize="12" Foreground="#E4E4E7" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <CheckBox x:Name="BatchWinCb" Content="Win"/>
                                    <CheckBox x:Name="BatchCtrlCb" Content="Ctrl"/>
                                    <CheckBox x:Name="BatchShiftCb" Content="Shift"/>
                                    <CheckBox x:Name="BatchAltCb" Content="Alt"/>
                                </StackPanel>
                                <ComboBox Grid.Column="1" x:Name="BatchKeyCombo" Width="80"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>

            <!-- Tab 3: AI Workspace -->
            <TabItem Header="AI Workspace">
                <StackPanel Margin="0,15,4,0">
                    <TextBlock Text="AI Provider &amp; Model Configuration" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC" Margin="0,0,0,10"/>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="AI Provider" FontSize="12" Foreground="#D4D4D8" Margin="0,0,0,5"/>
                            <ComboBox x:Name="ProviderCombo" SelectionChanged="ProviderCombo_SelectionChanged"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="Model Name" FontSize="12" Foreground="#D4D4D8" Margin="0,0,0,5"/>
                            <TextBox x:Name="ModelTextBox" ToolTip="Model Identifier (e.g. gemini-2.5-flash, gpt-4o, claude-3-5-sonnet-latest)"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="API Key" FontSize="12" Foreground="#D4D4D8" Margin="0,0,0,5"/>
                    <TextBox x:Name="ApiKeyTextBox" Margin="0,0,0,12" ToolTip="API Key for the selected provider"/>

                    <!-- Custom Endpoint (only visible for Custom OpenAI-Compatible) -->
                    <StackPanel x:Name="CustomEndpointPanel" Margin="0,0,0,12" Visibility="Collapsed">
                        <TextBlock Text="Custom Endpoint URL" FontSize="12" Foreground="#D4D4D8" Margin="0,0,0,5"/>
                        <TextBox x:Name="CustomEndpointTextBox" ToolTip="Custom API URL (e.g. http://localhost:11434/v1/chat/completions)"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="TestStatusText" Grid.Column="0" FontSize="11" Foreground="#A1A1AA" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" x:Name="TestConnectionBtn" Content="Test Connection" Style="{StaticResource SecondaryButton}" Click="TestConnectionBtn_Click"/>
                    </Grid>

                    <Separator Background="#27272A" Margin="0,5,0,10"/>

                    <!-- Prompt Customization -->
                    <TextBlock Text="Customize System Prompt" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC" Margin="0,0,0,5"/>
                    <TextBlock Text="This system instruction defines how clippings are processed by the AI." FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,8"/>
                    <TextBox x:Name="PromptTextBox" Height="180" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Margin="0,0,0,10"/>
                </StackPanel>
            </TabItem>

            <!-- Tab 4: Logs -->
            <TabItem Header="Logs">
                <Grid Margin="0,15,4,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Last Sync Logs" FontSize="14" FontWeight="SemiBold" Foreground="#C084FC"/>
                            <TextBlock Text="Log output from the last AI clippings sync operation." FontSize="11" Foreground="#A1A1AA" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Refresh Logs" Style="{StaticResource SecondaryButton}" Click="RefreshLogsBtn_Click" VerticalAlignment="Center"/>
                    </Grid>
                    
                    <TextBox Grid.Row="1" x:Name="LogsTextBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11" Background="#0F0F11" BorderBrush="#27272A" Margin="0,0,0,10" VerticalAlignment="Stretch"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#1E1E24" Padding="20,15" BorderThickness="0,1,0,0" BorderBrush="#27272A">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="100" Margin="0,0,10,0" Style="{StaticResource SecondaryButton}" Click="CancelButton_Click"/>
                <Button Content="Save Settings" Width="130" Style="{StaticResource PrimaryButton}" Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
